/* Generated by pipeline/desk_gadget.py — do not edit on-wiki. */
(function () {
  var DESK_URL = "/desk/";
  function openDesk(mode) {
    if (document.querySelector(".desk-overlay")) { return; }  // one overlay at a time (no stacking)
    var wrap = document.createElement("div");
    wrap.className = "desk-overlay desk-" + mode;
    var scrim = document.createElement("div"); scrim.className = "desk-scrim";
    var modal = document.createElement("div"); modal.className = "desk-modal";
    var close = document.createElement("button");
    close.className = "desk-close"; close.textContent = "\u2715"; close.title = "Close";
    var frame = document.createElement("iframe");
    frame.className = "desk-frame"; frame.src = DESK_URL; frame.title = "The Archivist's Desk";
    modal.appendChild(close); modal.appendChild(frame);
    wrap.appendChild(scrim); wrap.appendChild(modal);
    function dismiss() {
      if (wrap.parentNode) { document.body.removeChild(wrap); }
      document.removeEventListener("keydown", onKey);
    }
    function onKey(e) { if (e.key === "Escape") { dismiss(); } }
    scrim.addEventListener("click", dismiss);
    close.addEventListener("click", dismiss);
    document.addEventListener("keydown", onKey);
    document.body.appendChild(wrap);
  }
  mw.loader.using(["mediawiki.util"]).then(function () {
    var link = mw.util.addPortletLink("p-navigation", "#", "Ask the Books", "n-askbooks",
      "Search the books and ask questions");
    if (link) {
      link.addEventListener("click", function (e) { e.preventDefault(); openDesk("full"); });
    }
    if (mw.config.get("wgAction") === "view" && mw.config.get("wgNamespaceNumber") >= 0) {
      var fab = document.createElement("button");
      fab.className = "desk-fab"; fab.textContent = "Ask"; fab.title = "Ask the Books";
      fab.addEventListener("click", function () { openDesk("panel"); });
      document.body.appendChild(fab);
    }
  });
})();
